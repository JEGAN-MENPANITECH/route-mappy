// Sun profile removed — buildings now use a static neutral light
// to keep them consistently grey at all zoom levels and times.
// See style.json for the static light configuration.

export const SUN_DISABLED = true;
